---
name: list-projects
description: List Priogrid projects. Use when the user invokes /list-projects, /priogrid:list-projects, or asks to see known planning projects.
---

# list-projects

Run the bundled Priogrid script with the `list-projects` command:

```bash
node ../../scripts/priogrid.mjs list-projects
```

Resolve `../../scripts/priogrid.mjs` relative to this `SKILL.md`. Relay the markdown output in chat.
